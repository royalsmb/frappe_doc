# Copyright (c) 2024, Jokoor and contributors
# For license information, please see license.txt

import frappe
from frappe.model.document import Document


class RoomService(Document):
	def after_insert(self):
		if not frappe.db.exists('Item', self.service_name):
			item = frappe.new_doc('Item')
			item.item_code = self.service_name
			item.item_name = self.service_name
			item.item_group = 'Services'
			item.standard_rate = self.price
			item.is_purchase_item = 0
			item.save()
			frappe.db.commit()
